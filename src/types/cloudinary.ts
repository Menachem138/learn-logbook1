export interface CloudinaryResponse {
  publicId: string;
  url: string;
  resourceType: string;
  format: string;
  size: number;
}

export interface CloudinaryData {
  publicId: string;
  url: string;
  resourceType: string;
  format: string;
  size: number;
}